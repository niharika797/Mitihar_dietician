# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# ── Virtual Environment ───────────────────────────────────────────────────────
venv/
.venv/
env/

# ── Environment Variables & Secrets ─────────────────────────────────────────────────────
# NEVER commit .env files containing real secrets.
.env
.env.*
!.env.example
# Production env value reference (was .env.production) — contains real secrets,
# NOT read by the app; values injected into Cloud Run manually at deploy time.
deploy-env-reference.txt
mitihar-patient-app/.env
mitihar-patient-app/.env.*
!mitihar-patient-app/.env.example
mitihar-frontend/apps/.env
mitihar-frontend/apps/.env.*
!mitihar-frontend/apps/.env.example

# ── IDEs ──────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── Testing & Coverage ────────────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Credential & key files ───────────────────────────────────────────────────
docs/CREDENTIALS.local.md
client_secret_*.json
*service_account*.json
*firebase_service_account*.json
google-services.json
GoogleService-Info.plist
credentials.json
*.pem
*.p12
*.key

# ── Frontend — Node / pnpm ────────────────────────────────────────────────────
node_modules/
.pnpm-store/
*.pnpm-debug.log*
mitihar-frontend/.pnpm-store/

# ── Frontend — Build outputs ──────────────────────────────────────────────────
mitihar-frontend/apps/dist/
mitihar-frontend/apps/.vite/
mitihar-frontend/apps/build/

# ── Frontend — Type cache ─────────────────────────────────────────────────────
mitihar-frontend/apps/tsconfig.tsbuildinfo

# ── Design / Screens ──────────────────────────────────────────────────────────
Phone screens/
presentation/

# ── Nutrition source data (raw dumps — large binaries/JSON, not source code) ──
data/
IFCT2017.pdf

# ── Local variants ────────────────────────────────────────────────────────────
*.local

# ── Demo / audit credential store ─────────────────────────────────────────────
credentials/

# ── Checkpoint files (scripts, idempotent — no secrets but large/noisy) ───────
scripts/rename_checkpoint.json

# Internal docs and dev artifacts
docs/
logs/
credentials/
SPRINT_*.md
MITIHAR_GUIDEBOOK.md
RL_Roadmap.md
Task_List.md
implementation_plan.md
walkthrough.md
journal.txt
test_results_latest.txt
MITYAHAR_PREPRODUCTION_AUDIT.md
rename_checkpoint.json
scripts/*.md

# Expo cache
.expo/
mitihar-patient-app/.expo/

# Local databases
*.sqlite
*.db

# Sensitive local contracts/agreements
contract/

# Local tools and generated output
.playwright-mcp/
graphify-out/
app/graphify-out/

# Backup and temporary scripts
scripts/backup_food_items.py

# ── Database backups (shared with team — dev/test data only, no production data) ──
db-backups/*.sql.gz

# ── Uvicorn process logs (local dev only) ─────────────────────────────────────
uvicorn_*.txt

# ── Checkpoint files (large/noisy, idempotent — safe to re-run scripts) ───────
clean_dishes_llm_checkpoint.json
tag_medical_checkpoint.json

# ── Windows null device artifact ──────────────────────────────────────────────
NUL

# ── Docker smoke test env file ────────────────────────────────────────────────
smoke.env

# ── Local throwaway test files ────────────────────────────────────────────────
test_claude_cli.py
tests/test_output.txt

# ── Pre-migration DB dumps (local only) ───────────────────────────────────────
backups/

# ── Frontend dev-server logs (local dev only) ─────────────────────────────────
frontend_dev.txt
frontend_err.txt

# ── One-off debug scripts kept as untracked reference ─────────────────────────
scripts/debug/

# ── Performance test generated artifacts (contains plaintext test passwords) ──
tests/performance/test_manifest.json
tests/performance/reports/



# ── Local Claude Code config (machine-specific paths, personal hooks) ────────
.claude/*
!.claude/rules/
!.claude/rules/**
