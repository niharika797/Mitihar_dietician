# Mityahar Backend — dev/test-only dependencies (NOT installed in production image)
# pip-compile requirements-dev.in --output-file requirements-dev.lock
# Constrained against requirements.lock so shared transitive deps stay pinned identically.
-c requirements.lock

# Testing
pytest==9.0.3
